// app/page.tsx
'use client'

import Link from 'next/link'
import { useSession, signOut } from 'next-auth/react'
import Header from '@/components/header'
import Hero from '@/components/hero'
import Footer from '@/components/footer'
import FreelancerCard1 from '@/components/freelancer/FreelancerCard1'
import CategoryCard1 from '@/components/category/CategoryCard1'
import { categories } from '@/lib/constants'

export default function Home() {
  const { data: session, status } = useSession()

  // Примерни данни за специалисти (временно)
  const freelancers = [
    {
      id: 1,
      name: "Иван Иванов",
      profession: "Електротехник",
      rating: 4.8,
      reviews: 12,
      location: "София",
      hourlyRate: 25,
      jobSuccess: 98,
      img: "/images/avatars/1.jpg"
    },
    {
      id: 2,
      name: "Петър Петров",
      profession: "Водопроводчик",
      rating: 4.9,
      reviews: 24,
      location: "Пловдив",
      hourlyRate: 30,
      jobSuccess: 99,
      img: "/images/avatars/2.jpg"
    }
  ]

  return (
    <main className="min-h-screen bg-[#0D0D1A]">
      {/* Header от Freeio */}
      <Header />

      {/* Hero от Freeio (вече адаптиран) */}
      <Hero />

      {/* Категории */}
      <section className="container mx-auto px-4 py-12">
        <h2 className="text-2xl font-bold text-white mb-8">Категории услуги</h2>
        <div className="grid grid-cols-2 md:grid-cols-4 gap-4">
          {categories.map((category) => (
            <CategoryCard1 key={category.id} data={{
              icon: category.icon,
              title: category.name,
              skill: "10+ skills"
            }} />
          ))}
        </div>
      </section>

      {/* Популярни специалисти */}
      <section className="container mx-auto px-4 py-12">
        <h2 className="text-2xl font-bold text-white mb-8">Популярни специалисти</h2>
        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
          {freelancers.map((freelancer) => (
            <FreelancerCard1 key={freelancer.id} data={freelancer} />
          ))}
        </div>
      </section>

      {/* CTA секция */}
      <section className="container mx-auto px-4 py-12">
        <div className="bg-gradient-to-r from-[#1DB954] to-[#169b43] rounded-lg p-8 text-center">
          <h2 className="text-3xl font-bold text-white mb-4">Печели с ProZona</h2>
          <p className="text-white text-lg mb-6">Намери бързо и лесно клиенти във твоя град</p>
          <Link
            href="/for-specialists"
            className="inline-block px-8 py-4 bg-white text-[#1DB954] font-semibold rounded-lg hover:bg-gray-100 transition-colors"
          >
            Започни да печелиш
          </Link>
        </div>
      </section>

      {/* Footer от Freeio */}
      <Footer />
    </main>
  )
}